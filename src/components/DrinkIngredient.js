import { Text } from '@chakra-ui/react'

const DrinkIngredient = ({ ingredient }) => {
    return (
        <>
            <Text fontSize="lg">{ingredient}</Text>
        </>
    )
}

export default DrinkIngredient